# `client/specs`

Written specs for `@devdigest/web` screens/flows, to implement against before
or alongside code — not test fixtures (those are colocated `*.test.tsx`).

Empty for now; add one file per feature spec.
