/* Root — thin RSC entry. View lives in _components/HomeRedirectView. */
import { HomeRedirectView } from "./_components/HomeRedirectView/HomeRedirectView";

export default function HomePage() {
  return <HomeRedirectView />;
}
