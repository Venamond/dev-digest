/** Constants for the Skills list view. */

/** Card grid template (responsive auto-fill). */
export const CARD_GRID_COLS = "repeat(auto-fill, minmax(280px, 1fr))";

/** Skill types offered in the create form (matches SkillType). */
export const SKILL_TYPE_VALUES = ["rubric", "convention", "security", "custom"] as const;
