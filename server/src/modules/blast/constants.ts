/** Prior PRs listed on the card ("Prior PRs touching these files"). */
export const MAX_PRIOR_PULLS = 5;

/** Dismissed findings shown per prior PR — enough to notice, not a review. */
export const MAX_FINDINGS_PER_PRIOR_PULL = 3;
