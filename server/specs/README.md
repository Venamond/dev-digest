# `server/specs`

Written specs/contracts for `@devdigest/api` features, to implement against
before or alongside code — not test fixtures (those live under `test/`).

Empty for now; add one file per feature spec.
