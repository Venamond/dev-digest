# `server/docs`

Architecture decision records and design notes for `@devdigest/api` that don't
belong in the always-loaded `CLAUDE.md` — link them from there under "Read
when" instead of inlining detail.

Empty for now; add one file per decision (e.g. `adr-0001-run-executor-split.md`).
