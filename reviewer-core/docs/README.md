# `reviewer-core/docs`

Architecture decision records and design notes for `@devdigest/reviewer-core`
that don't belong in the always-loaded `AGENTS.md` — link them from there
under "Read when" instead of inlining detail.

Empty for now; add one file per decision.
