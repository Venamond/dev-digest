# `reviewer-core/specs`

Written specs for `@devdigest/reviewer-core` behavior (prompt assembly,
grounding rules, scoring), to implement against before or alongside code —
not test fixtures (those live under `test/`).

Empty for now; add one file per feature spec.
