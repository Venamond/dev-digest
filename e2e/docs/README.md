# `e2e/docs`

Architecture decision records and design notes for `@devdigest/e2e` that
don't belong in the always-loaded `AGENTS.md` — link them from there under
"Read when" instead of inlining detail.

Empty for now; add one file per decision.
